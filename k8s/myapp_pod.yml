apiVersion: v1
kind: Pod

metadata:
  name: myapp-pod

spec:
  containers:
    - name: myapp-pod
      image: jogle1/node-todo-app:latest
      ports:
        - containerPort: 8000
      imagePullPolicy: Always
